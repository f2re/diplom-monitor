# DiplomMonitor 🎓

Веб-приложение для визуализации прогресса работы над дипломом. Помогает студентам осознать оставшееся время через сетку недель и мотивирует не пропускать этапы подготовки.

## ✨ Особенности
- **Сетка недель**: Визуализация времени от начала работы до защиты.
- **Глобальное управление (Admin System)**: 
  - Первый зарегистрированный пользователь автоматически становится **Администратором**.
  - Только Администратор устанавливает глобальные даты (начало и дедлайн защиты) для всех пользователей.
  - Только Администратор управляет глобальными специальными периодами (отпуска, командировки).
- **Умная раскраска и расчет**: 
  - `Зеленый`: Неделя закрыта успешно.
  - `Черный`: Пропущенная неделя (прошлые недели без отметки).
  - `Синий`: Текущая неделя.
  - `Желтый`: Специальные периоды (отпуска, стажировки).
  - **Автовычитание**: Система автоматически вычитает недели специальных периодов из общего срока, отображая "чистое" (эффективное) количество рабочих недель.
- **Telegram Auth**: Быстрый вход через Telegram Login Widget.
- **Уведомления**: Еженедельные напоминания в Telegram по воскресеньям, если неделя не закрыта.
- **Групповой вид**: Возможность просматривать сетки других пользователей (только чтение).
- **Ограничения**: Регулярные пользователи могут редактировать только свое имя, эмодзи и отмечать прогресс текущей недели. Глобальный график и отпуска фиксированы администратором.

## 🚀 Быстрый старт (Docker)

### 1. Подготовка окружения
Клонируйте репозиторий. Для работы Telegram-авторизации и уведомлений необходимо настроить бота.

#### Настройка Telegram бота:
1. Создайте бота через [@BotFather](https://t.me/BotFather) и получите **API Token**.
2. В @BotFather используйте команду `/setdomain`, выберите вашего бота и укажите домен (для локальной разработки: `http://localhost` или `http://127.0.0.1`).
3. Скопируйте `backend/.env.example` в `backend/.env` и вставьте ваш токен:
   ```bash
   cp backend/.env.example backend/.env
   # Отредактируйте .env и установите TELEGRAM_BOT_TOKEN
   ```
4. Обновите имя бота во фронтенд-компоненте `frontend/src/components/AuthForm.vue`:
   Замените `weeks_until_diploma_bot` на username вашего бота.

### 2. Запуск системы
Система полностью контейнеризирована. Для корректной работы сетевого взаимодействия на Linux (особенно для корректного проброса `localhost` и виджета Telegram) рекомендуется использовать режим `network_mode: host`, который уже настроен в `docker-compose.yml`.

Запустите все сервисы одной командой:

```bash
sudo docker-compose up --build -d
```

**Важные команды для управления:**
- **Перезапуск с очисткой кэша (если сайт не открывается):**
  ```bash
  sudo docker-compose down && sudo docker-compose up --build -d
  ```
- **Просмотр логов в реальном времени:**
  ```bash
  sudo docker-compose logs -f
  ```

Это поднимет:
- **PostgreSQL**: База данных (порт 5432).
- **Backend (FastAPI)**: API и планировщик уведомлений (порт 8000).
- **Frontend (Vue 3)**: Интерфейс (порт 5173).

### 3. Применение миграций
После запуска базы данных обязательно примените миграции Alembic для создания структуры таблиц:

```bash
sudo docker-compose exec backend alembic upgrade head
```

## 🛠 Тестирование

### Доступ к интерфейсу
Откройте [http://127.0.0.1:5173](http://127.0.0.1:5173) в браузере.
*Рекомендуется использовать именно IP `127.0.0.1`, чтобы избежать проблем с IPv6/IPv4 разрешением `localhost`.*

### Проверка API (Swagger)
Документация API доступна по адресу: [http://127.0.0.1:8000/docs](http://127.0.0.1:8000/docs)

## ❓ Устранение неполадок

### Сайт долго грузится или "Empty reply from server"
На некоторых дистрибутивах Linux стандартный Docker Bridge может работать нестабильно. 
1. Убедитесь, что в `docker-compose.yml` для всех сервисов указано `network_mode: host`.
2. Попробуйте зайти через `http://127.0.0.1:5173` вместо `localhost`.
3. Очистите кэш Vite: `rm -rf frontend/node_modules/.vite`.

### Ошибка: Permission denied (Docker)
Если вы получаете `PermissionError` при запуске `docker-compose`, выполните команду с `sudo`:
```bash
sudo docker-compose up --build -d
```
Или добавьте своего пользователя в группу `docker`:
```bash
sudo usermod -aG docker $USER
```
*(После этого необходимо перезайти в систему)*.

## 📂 Структура проекта
- `backend/app/models`: Модели SQLAlchemy (User, WeekProgress, SpecialPeriod).
- `backend/app/api`: Эндпоинты для авторизации, работы с сеткой и профилями.
- `backend/app/core/notifications.py`: Логика еженедельных напоминаний.
- `frontend/src/components`: Vue-компоненты (Сетка, Ячейка, Формы).
- `frontend/src/stores`: Pinia сторы для управления состоянием.

## 📝 Лицензия
MIT