# Weeks Until Diploma (Недель до Диплома) 🎓

Минималистичное веб-приложение для визуализации времени, оставшегося до защиты диплома. Каждая неделя — это квадрат; каждый квадрат — это возможность.

## 🌟 Видение
Проект создан для того, чтобы помочь студентам осознать конечность времени и мотивировать их к регулярной работе. Сетка недель наглядно показывает путь от начала работы до финальной защиты, превращая абстрактный дедлайн в осязаемый график.

## 🎯 Основные цели
- **Визуализация времени**: Сетка, представляющая путь от старта до защиты диплома.
- **Микро-прогресс**: Удобная отметка достижений за неделю.
- **Осознанность**: Помощь в отслеживании графика без лишнего стресса.

## 🛠 Технологический стек
### Backend
- **FastAPI** (Python 3.11+)
- **SQLAlchemy 2.0+** (ORM) & **Alembic** (миграции)
- **Pydantic v2** (валидация данных)
- **JWT** (аутентификация)
- **PostgreSQL** (база данных)

### Frontend
- **Vue.js 3** (Composition API, `<script setup>`)
- **Vite** (инструмент сборки)
- **Tailwind CSS** (стилизация)
- **Pinia** (управление состоянием)
- **Lucide Vue** (иконки)

## 🏗 Структура проекта
```text
diplom-monitor/
├── backend/                # Приложение FastAPI
│   ├── app/
│   │   ├── api/            # API маршруты
│   │   ├── core/           # Конфигурация, безопасность
│   │   ├── models/         # Модели SQLAlchemy
│   │   ├── schemas/        # Схемы Pydantic
│   │   ├── services/       # Бизнес-логика
│   │   └── main.py         # Точка входа
│   ├── migrations/         # Миграции Alembic
│   └── tests/              # Тесты Pytest
├── frontend/               # Приложение Vue.js
│   ├── src/
│   │   ├── api/            # Клиенты API
│   │   ├── components/     # UI компоненты
│   │   ├── composables/    # Повторно используемая логика
│   │   ├── stores/         # Хранилища Pinia
│   │   └── views/          # Компоненты страниц
│   └── tests/              # Тесты Vitest
├── shared/                 # Общие константы/типы
├── docker-compose.yml      # Окружение для разработки
├── plan.md                 # Детальный план разработки
└── gemini.md               # Руководство по проекту
```

## 🚀 План разработки (Roadmap)
1.  **Фаза 1: Фундамент**: Настройка структуры проекта, Docker и базовой аутентификации.
2.  **Фаза 2: Ядро**: Логика расчета недель на бэкенде и отрисовка сетки на фронтенде.
3.  **Фаза 3: Взаимодействие**: Возможность отмечать недели, добавлять заметки и синхронизировать данные с БД.
4.  **Фаза 4: Персонализация**: Настройки профиля, установка своих дат и выбор эмодзи.
5.  **Фаза 5: Полировка**: Анимации, поддержка PWA и аудит доступности.

## 📐 Стандарты разработки
- **KISS (Keep It Simple, Stupid)**: Избегаем избыточного усложнения.
- **DRY (Don't Repeat Yourself)**: Выносим общую логику в сервисы и composables.
- **Mobile-First**: Дизайн и реализация в первую очередь для мобильных устройств (от 320px).
- **Type Safety**: Обязательное использование подсказок типов в Python.

---
Проект находится в стадии активной разработки.
