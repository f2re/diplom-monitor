# DiplomMonitor 🎓

Веб-приложение для визуализации прогресса работы над дипломом. Помогает студентам осознать оставшееся время через сетку недель и мотивирует не пропускать этапы подготовки.

## ✨ Особенности
- **Сетка недель**: Визуализация времени от начала работы до защиты.
- **Умная раскраска**: 
  - `Зеленый`: Неделя закрыта успешно.
  - `Черный`: Пропущенная неделя (прошлые недели без отметки).
  - `Синий`: Текущая неделя.
  - `Желтый`: Специальные периоды (отпуск, командировка).
- **Telegram Auth**: Быстрый вход через Telegram Login Widget.
- **Уведомления**: Еженедельные напоминания в Telegram по воскресеньям, если неделя не закрыта.
- **Групповой вид**: Возможность просматривать сетки других пользователей (только чтение).
- **Ограничения**: Редактировать можно только свою сетку и только текущую неделю.

## 🚀 Быстрый старт (Docker)

### 1. Подготовка окружения
Клонируйте репозиторий и создайте файл `.env` в корне проекта (или настройте переменные в `docker-compose.yml`):

```bash
# Telegram настройки
TELEGRAM_BOT_TOKEN=your_bot_token_from_botfather
```

### 2. Запуск системы
Система полностью контейнеризирована. Запустите все сервисы одной командой:

```bash
docker-compose up --build -d
```

Это поднимет:
- **PostgreSQL**: База данных (порт 5432).
- **Backend (FastAPI)**: API и планировщик уведомлений (порт 8000).
- **Frontend (Vue 3)**: Интерфейс (порт 5173).

### 3. Применение миграций
После запуска базы данных примените миграции Alembic:

```bash
docker-compose exec backend alembic upgrade head
```

## 🛠 Тестирование

### Доступ к интерфейсу
Откройте [http://localhost:5173](http://localhost:5173) в браузере.

### Проверка API (Swagger)
Документация API доступна по адресу: [http://localhost:8000/docs](http://localhost:8000/docs)

### Примеры команд для локального тестирования бэкенда (без Docker)

Если вы хотите запустить тесты или бэкенд локально:

1. **Установка зависимостей**:
   ```bash
   cd backend
   pip install -r requirements.txt
   ```

2. **Запуск тестов**:
   ```bash
   export PYTHONPATH=$PYTHONPATH:.
   pytest tests/test_api_v1.py
   ```

3. **Запуск планировщика**:
   Напоминания отправляются автоматически при запуске бэкенда. Чтобы протестировать отправку не дожидаясь воскресенья, можно вызвать задачу `send_reminders` вручную в коде или через временный эндпоинт.

## 🤖 Настройка Telegram Бота
1. Создайте бота в [@BotFather](https://t.me/BotFather).
2. Выполните команду `/setdomain` и укажите ваш домен (для локальной разработки это может потребовать ngrok или правки `hosts`, так как Telegram требует валидный домен для виджета).
3. Вставьте полученный токен в `TELEGRAM_BOT_TOKEN`.

## ❓ Устранение неполадок

### Ошибка: Permission denied (Docker)
Если вы получаете `PermissionError` при запуске `docker-compose`, выполните команду с `sudo`:
```bash
sudo docker-compose up --build -d
```
Или добавьте своего пользователя в группу `docker`:
```bash
sudo usermod -aG docker $USER
```
*(После этого необходимо перезайти в систему)*.

## 📂 Структура проекта
- `backend/app/models`: Модели SQLAlchemy (User, WeekProgress, SpecialPeriod).
- `backend/app/api`: Эндпоинты для авторизации, работы с сеткой и профилями.
- `backend/app/core/notifications.py`: Логика еженедельных напоминаний.
- `frontend/src/components`: Vue-компоненты (Сетка, Ячейка, Формы).
- `frontend/src/stores`: Pinia сторы для управления состоянием.

## 📝 Лицензия
MIT